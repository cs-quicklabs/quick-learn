import React from 'react';
import CourseTable from './CourseTable';

export const metadata = {
  title: 'Orphan Courses • Quick Learn',
  description: 'Orphan Course quick learn',
};

const page = () => {
  return <CourseTable />;
};

export default page;
