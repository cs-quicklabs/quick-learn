import React from 'react';
import LessonDetails from './lessonDetails';

export const metadata = {
  title: 'Lesson Details • Quick Learn',
  description: 'Lesson Details quick learn',
};

const page = () => {
  return <LessonDetails />;
};

export default page;
