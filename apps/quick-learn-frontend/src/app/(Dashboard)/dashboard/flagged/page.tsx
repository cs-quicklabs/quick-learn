import React from 'react';
import FlaggedList from './flaggedList';

export const metadata = {
  title: 'Flagged • Quick Learn',
  description: 'Flagged quick learn',
};

const page = () => {
  return <FlaggedList />;
};

export default page;
