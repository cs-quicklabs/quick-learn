import React from 'react';
import ViewArchivedUser from './ViewArchivedUser';

export const metadata = {
  title: 'Archived User • Quick Learn',
  description: 'Archived User Path quick learn',
};

const page = () => {
  return (
    <div>
      <ViewArchivedUser />
    </div>
  );
};

export default page;
