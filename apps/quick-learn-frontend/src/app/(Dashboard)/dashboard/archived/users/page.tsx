import React from 'react';
import ArchivedUsers from './ArchivedUsers';

export const metadata = {
  title: 'Archived Users • Quick Learn',
  description: 'Archived users quick learn',
};

const page = () => {
  return <ArchivedUsers />;
};

export default page;
