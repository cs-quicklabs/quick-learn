import React from 'react';
import ProfileSettings from './ProfileSettings';

export const metadata = {
  title: 'Profile Settings • Quick Learn',
  description: 'Profile settings quick learn',
};

const page = () => {
  return <ProfileSettings />;
};

export default page;
