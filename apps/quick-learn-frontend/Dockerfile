# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build quick-learn-frontend`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t quick-learn-frontend`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system quick-learn-frontend && \
          adduser --system -G quick-learn-frontend quick-learn-frontend

COPY apps/quick-learn-frontend/ quick-learn-frontend/
RUN chown -R quick-learn-frontend:quick-learn-frontend .

COPY package.json quick-learn-frontend/

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix quick-learn-frontend --omit=dev -f install

CMD [ "node", "quick-learn-frontend" ]
